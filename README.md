# NodeJS_Express_CRUD_with_MongoDB

# To start

```
npm i
cp .env.example .env
cd mongodb
docker-compose up -d
np run dev
```

# Commands

```
GET http://localhost:8000/api/muestras
GET BY ID
GET BY ANOMALÍA
GET BY USUARIO
PUT
POST
DELETE
```
